<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HSCK98 | Login</title>
    <%- include("partials/links.ejs") %>
    <link rel="stylesheet" href="styles/login.css">
  </head>
  <body>
    <div class="login-container d-flex justify-content-center align-items-center">
        <div class="login-slogan">
          <h1>Narrator</h1>
          <h2>Share your adventures and track your journey</h2>
        </div>
        <div class="login-form">
          <form action="/login" method="POST">
            <div class="login-username form-group">
            <% if(locals.username) { %>
              <input id="username" class="form-control" name="username" type="text" value="<%= username %>" required>
            <% } else { %>
              <input id="username" class="form-control" name="username" type="text" placeholder="Username" required>
            <% } %>
            </div>
            <div class="login-password form-group">
              <input id="password" class="form-control" name="password" type="password" placeholder="Password" required>
            </div>
            <div class="login-submit d-flex justify-content-end form-group">
              <input class="submit-login btn btn-primary align-self-end form-control" type="submit" value="Submit">
            </div>
            <div class="login-register form-group">
              <p>New to Narrator? <a href="/register">Register</a></p>
            </div>
          </form>

          <% if(locals.error) { %>
            <p class="error"><%= error %></p>
          <% } %>
      </div>
    </div>
  </body>
</html>